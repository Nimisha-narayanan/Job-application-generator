# Job-application-generator
 A Gen AI application that scrapes job postings, extracts structured job data using LLMs, matches required skills with personal projects using TF-IDF and cosine similarity, and generates personalized job application emails and mock interview questions.
